apiVersion: nais.io/v1alpha1
kind: Alert
metadata:
  name: spleis-faketestdata-alerts
  labels:
    team: tbd
spec:
  receivers:
    slack:
      channel: '#team-bømlo-alerts'
  alerts:
    - alert: spleis-faketestdata-har-ingen-aktive-pods
      expr: up{app=~"spleis-faketestdata",job="kubernetes-pods"} == 0
      for: 2m
      description: spleis-faketestdata mangler aktive pods i preprod!
      action: Se `kubectl describe pod {{ $labels.kubernetes_pod_name }}` for events,
        og `kubectl logs {{ $labels.kubernetes_pod_name }}` for logger
      sla: respond within 1h, during office hours
      severity: danger
